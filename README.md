# SmartCafe ☕

A clean and simple **Cafe Management System** built with Python.

## Features

- Clean menu management (Beverages, Food, Desserts, Snacks)
- Take customer orders with real-time calculation
- Persistent data storage (JSON)
- View order history
- Revenue tracking
- Professional object-oriented design

## How to Run

```bash
# 1. Clone or download the project
cd SmartCafe

# 2. Install dependencies (if any)
pip install -r requirements.txt

# 3. Run the application
python -m smartcafe.main 
```

## File Structure
```bash
SmartCafe/
├── smartcafe/         # Replaces both 'src/' and 'include/'
│   ├── __init__.py    # Makes this folder a recognizable Python module
│   ├── main.py        # The entry point of your app
│   └── core_logic.py  # Your classes and functions
├── tests/             # Standard practice to keep tests separate
├── build/             # Used if you are packaging the app
├── dist/              # Contains the final packaged executable or library
├── requirements.txt   # Lists external dependencies
├── README.md
└── .gitignore
```

## Author 
Haroon Khalid